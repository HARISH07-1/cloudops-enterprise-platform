stage('Deploy Container') {
    steps {
        bat 'docker stop cloudops-container || echo container_not_running'
        bat 'docker rm cloudops-container || echo container_not_present'
        bat 'docker run -d -p 8081:80 --name cloudops-container cloudops-html-app'
    }
}
